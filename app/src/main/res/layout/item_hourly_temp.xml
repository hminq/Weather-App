<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center_horizontal">

    <!-- Tooltip body with top/side shadow only -->
    <FrameLayout
        android:layout_width="@dimen/hourly_temp_width"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_tooltip_shadow">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="3dp"
            android:layout_marginEnd="3dp"
            android:layout_marginTop="3dp"
            android:padding="12dp"
            android:background="@drawable/bg_tooltip_content">

        <TextView
            android:id="@+id/tvTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:text="10:15 AM"
            android:textSize="@dimen/label3_text_size"
            android:textColor="?attr/colorOnSurfaceVariant"/>

        <androidx.constraintlayout.helper.widget.Flow
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvTime"
                android:layout_marginTop="4dp"
            app:constraint_referenced_ids="ivCondition, tvTemperature"
            app:flow_wrapMode="chain"
                app:flow_horizontalGap="4dp"/>

        <ImageView
            android:id="@+id/ivCondition"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_cloudy"
            app:tint="?attr/colorOnSurfaceVariant" />

        <TextView
            android:id="@+id/tvTemperature"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/hourly_weather_temp_placeholder"
            android:textColor="?attr/colorOnSurface"
                android:textSize="@dimen/body1_text_size"
                android:textStyle="bold"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
    </FrameLayout>

    <!-- Triangle pointing down -->
    <ImageView
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_marginTop="-4dp"
        android:rotation="180"
        android:src="@drawable/ic_triangle_down_filled"
        app:tint="?attr/colorSurface"/>

</LinearLayout>
